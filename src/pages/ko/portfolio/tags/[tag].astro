---
import TagArchivePage from '../../../../components/TagArchivePage.astro';
import { getPortfolioTagPaths } from '../../../../utils/tags';

export const getStaticPaths = () => getPortfolioTagPaths('ko');

const { tag, tagLabel, entries } = Astro.props;
---

<TagArchivePage lang="ko" kind="portfolio" tag={tag} tagLabel={tagLabel} entries={entries} />
