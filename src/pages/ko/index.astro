---
// Korean home (/ko/). Thin wrapper over the shared HomePage (Stage 13).
// Creating this route is what makes the header/footer language toggle's `/ko/`
// target resolve.
import HomePage from '../../components/HomePage.astro';
---

<HomePage lang="ko" />
