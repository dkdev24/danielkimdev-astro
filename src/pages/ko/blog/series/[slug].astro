---
import SeriesArchivePage from '../../../../components/SeriesArchivePage.astro';
import { getSeriesArchivePaths } from '../../../../utils/series';

export const getStaticPaths = () => getSeriesArchivePaths('ko');

const { slug, title, description, entries } = Astro.props;
---

<SeriesArchivePage lang="ko" slug={slug} title={title} description={description} entries={entries} />
