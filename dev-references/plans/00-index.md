# Build Plan — Index (P0, session-sized stages)

> Decomposition of [`../astro-site-prd.md`](../astro-site-prd.md) into stages each scoped to **one ~20–30 min continuous-work session**. Scope here is **P0 only** (ship v1). P1/P2 are listed as a backlog at the bottom, not yet broken into sessions.

## How to use this

1. Work stages **in numeric order** — later stages assume earlier ones exist (see Depends-on in each file).
2. Open one stage doc, do only that session, check off its task boxes, confirm its **Acceptance criteria**, run its **Verify** step.
3. At session end, follow the project convention: update [`../../HANDOFF.md`](../../HANDOFF.md) and append to [`../../WORKLOG.md`](../../WORKLOG.md). Each stage's **Handoff note** says what to record.
4. Best run in **Claude Code** (local toolchain — Cowork's sandbox can't run `astro build`/`dev` against this repo's macOS `node_modules`).
5. If a stage runs long, stop at the last completed task box and note the cut-line in HANDOFF — the boxes are the natural resume points.

## Conventions for every stage

- Prefer the simplest Astro-native approach; leave `TODO(daniel):` rather than inventing facts (PRD §9 / §13).
- Design **values** come from [`../DESIGN-minimax.md`](../DESIGN-minimax.md); the PRD defines scope, a11y, and i18n. On aesthetic conflict, DESIGN-minimax wins; on scope/a11y/i18n, PRD wins (PRD §8).
- Both locales (EN root, KO `/ko/`) and both themes (light/dark) are first-class — every UI stage covers all four combinations.
- Verify with `astro check` + a local `astro dev`/`astro build`; keep Lighthouse for the dedicated perf stage.

## Stages

**Status legend:** ⬜ Not started · 🟡 In progress · ✅ Done. Update the cell when you pick up / finish a stage.

| # | Stage | Phase (PRD §16) | Depends on | Status |
|---|-------|-----------------|------------|--------|
| 01 | [Foundation config & repo hygiene](stage-01-foundation-config.md) | 1 Foundation | — | ✅ Done |
| 02 | [Design tokens — light](stage-02-design-tokens-light.md) | 1 Foundation | 01 | ✅ Done |
| 03 | [Dark mode token set & theme switching](stage-03-dark-mode.md) | 1 Foundation | 02 | ✅ Done |
| 04 | [Typography & font wiring](stage-04-typography-fonts.md) | 1 Foundation | 02 | ✅ Done |
| 05 | [i18n utilities & UI dictionaries](stage-05-i18n-utilities.md) | 1 Foundation | 01 | ✅ Done |
| 06 | [Base layout shell & landmarks](stage-06-base-layout.md) | 1 Foundation | 03, 04, 05 | ✅ Done |
| 07 | [Header: nav + language & theme toggles](stage-07-header-nav-toggles.md) | 1 Foundation | 06 | ✅ Done |
| 08 | [Footer & global chrome wiring](stage-08-footer-chrome.md) | 1 Foundation | 07 | ✅ Done |
| 09 | [Content collections & schemas](stage-09-content-collections.md) | 2 Content engine | 01 | ✅ Done |
| 10 | [Seed content (EN/KO posts, portfolio, timeline)](stage-10-seed-content.md) | 2 Content engine | 09 | ✅ Done |
| 11 | [Core components A — Button, Card, Tag](stage-11-components-core-a.md) | 2 Content engine | 03, 04 | ✅ Done |
| 12 | [Core components B — Callout, Timeline, Code, TOC](stage-12-components-core-b.md) | 2 Content engine | 11 | ✅ Done |
| 13 | [Home page (both locales)](stage-13-home.md) | 3 Pages | 08, 10, 11 | ✅ Done |
| 14 | [About page (both locales)](stage-14-about.md) | 3 Pages | 10, 12 | ✅ Done |
| 15 | [Portfolio page + filter](stage-15-portfolio.md) | 3 Pages | 10, 11 | ✅ Done |
| 16 | [Blog index + filter](stage-16-blog-index.md) | 3 Pages | 10, 11 | ✅ Done |
| 17 | [Blog post layout + MDX features](stage-17-blog-post.md) | 3 Pages | 12, 16 | ✅ Done |
| 18 | [SEO, feeds, sitemap, structured data](stage-18-seo-feeds.md) | 3 Pages | 13, 17 | ✅ Done |
| 19 | [Accessibility pass (WCAG 2.1 AA)](stage-19-accessibility.md) | 4 Polish | 13–18 | ✅ Done |
| 20 | [Performance & Lighthouse pass](stage-20-performance.md) | 4 Polish | 13–18 | ✅ Done |
| 21 | [Deploy to Cloudflare Pages + README](stage-21-deploy.md) | 5 Ship | 19, 20 | ⬜ Not started |

> **Note:** Stage 01 completed on 2026-06-27 — config finalized (`output: 'static'` added), `.nvmrc` pinned to Node 22.12.0, `consts.ts` populated, `public/_headers` added, starter posts + Atkinson fonts removed. Verified with `astro check` (0 errors) and `npm run build` (succeeds).
>
> **Note:** Stage 02 completed on 2026-06-28 — light design-token layer landed in `src/styles/tokens.css` (color/spacing/radius/shadow/type/motion, every value traced to `DESIGN-minimax.md`); `global.css` now `@import`s it and base resets read tokens only. Legacy Bear Blog vars kept as a deprecated block for un-reworked scaffold (remove in Stages 07/08/16/17). Verified `astro check` (0 errors) + `astro build` (succeeds).
>
> **Note:** Stage 03 completed on 2026-06-28 — dark token set added as a `[data-theme="dark"]` override in `tokens.css` (near-black surfaces, inverted text ramp, lighter brand/link blue, blue-shifted glow, deepened shadows; `color-scheme` per theme). No-flash resolver `src/components/ThemeScript.astro` (is:inline, reads `localStorage.theme` → falls back to `prefers-color-scheme`) wired into `BaseHead` head. Toggle contract documented for Stage 07 (key `theme`, attr `data-theme`, `theme-change` event). All dark text/bg pairs ≥AA (lowest 5.15:1). Verified `astro check` + `astro build`; inline script + dark selector confirmed in `dist`.
>
> **Note:** Stage 15 completed on 2026-06-28 — **Portfolio live in both locales.** Shared `PortfolioPage.astro` (fed by `lang`): collection-driven responsive grid (1→2→3 col) of `Card`s with role·org·period meta, summary, `Tag`s, and an **inline expandable `<details>`** rendering each entry's body + links (+ a career→About-timeline cross-link) — keyboard-operable, works with no JS, no `/portfolio/[slug]` route (P1). A **tag filter** chip row (interactive `Tag`s + an "All") filters client-side by toggling `[hidden]`; degrades to the full list without JS. Thin wrappers `pages/portfolio.astro` + new `pages/ko/portfolio.astro`. **This resolves the Home "View portfolio" CTA + featured-card links** (no longer 404). Verified live (Playwright): filter 6→2 for `drm-content-security` (aria-pressed set) → All resets to 6; 6 expandable details; covers all 4 buckets. `astro check` 0 errors. Next: Stage 16 (Blog index) → 17 (Blog post).
>
> **Note:** Stage 14 completed on 2026-06-28 — **About live in both locales.** Shared `AboutPage.astro` (fed by `lang`): headshot with a **monogram fallback** (`<img onerror="this.remove()">` reveals a "DK" circle — verified: missing image → monogram shows), narrative bio (`data/about.ts`), grouped skills (Product/Domain/Technical/AI from `ABOUT_SKILLS`, localized group labels), a **data-driven career timeline** (the `timeline` collection via `TimelineItem` in `<ol class="timeline">` — adding an entry needs no markup edits), and a contact row (email + LinkedIn `Button`s). Thin wrappers `pages/about.astro` (replaces the legacy lorem/BlogPost page) + new `pages/ko/about.astro`. Added `about.skillsTitle` to both dicts. Verified live (Playwright): 4 timeline items + 4 skill groups render, monogram fallback works, KO route shows KO skills. `astro check` 0 errors. KO bio + timeline dates/orgs remain `TODO(daniel)`. Next: Stage 15 (Portfolio) / 16 (Blog index).
>
> **Note:** Stage 13 completed on 2026-06-28 — **Home live in both locales** (Phase 3 begins). A shared `HomePage.astro` (fed by `lang`) renders hero (name/tagline/subhead + View-portfolio / Read-the-blog CTAs + identity chips from `HOME_HERO.focusAreas`), **featured work** (3 `featured` portfolio cards, current locale, ordered), **latest writing** (3 newest non-draft posts, current locale, with date · reading-time, graceful empty state), and an About teaser. Thin wrappers `pages/index.astro` (en) + **new `pages/ko/index.astro`** (ko) — the KO route now exists, so the header/footer language toggle's `/ko/` target resolves. Content pulled from collections (not hardcoded); locale filtering verified (EN home → EN posts, KO → KO). **Fixed a global bug:** added the missing `box-sizing: border-box` reset to `global.css` — `.container` was overflowing the viewport (caused 32px horizontal scroll at 375px); now no overflow. Verified EN+KO in light+dark and at 375px via Playwright. `astro check` 0 errors. NOTE: hero "View portfolio" CTA + featured cards link to `/portfolio/` (404s until Stage 15); post links use `/blog/<id>/` (legacy scheme, finalized 16/17). Next: Stage 14 (About) / 15 (Portfolio) / 16 (Blog index).
>
> **Note:** Stage 12 completed on 2026-06-28 — content-rich components, all verified in light + dark via Playwright. **`Callout.astro`** — note/tip/warning/important variants on the semantic status tokens (AA in both themes; accent rail+icon, heading-color title, primary-text body). **`TimelineItem.astro`** — vertical-rail entry (brand dot, rail stops after last) rendered inside `<ol class="timeline">`; props `title`/`org?`/`period?` + slot. **`TOC.astro`** — built from Astro `headings`, `<details open>` (works with no JS) + IntersectionObserver scroll-spy `is-active` highlight; sticky ≥1024px. **`CodeCopy.astro`** — runtime enhancer that wraps each Shiki `<pre.astro-code>`, adds a `data-language` label + a keyboard copy button with `aria-live` announce (Clipboard API). Config: dual Shiki themes (`github-light`/`github-dark`) swapped under `[data-theme="dark"]` in `global.css` + a transformer stamping `data-language`; added a global `prefers-reduced-motion` kill-switch and `.code-block*` styles. Code-block chrome lives in `global.css` (runtime nodes). `astro check` 0 errors. **All page stages (13–17) now unblocked.**
>
> **Note:** Stage 11 completed on 2026-06-28 — three high-reuse UI primitives, all token-only (no literals), verified in light + dark via Playwright screenshots. **`Button.astro`** — variants primary (brand-filled, theme-safe) / secondary (muted + hairline) / ghost; renders `<a>` (href) or `<button>`; `size` md(≥44px)/sm; `disabled`, `external`; states hover/active/focus-visible/disabled. **`Card.astro`** — slot-based surface (radius-xl, Level-1 shadow); `featured` (brand purple-glow + brand hairline), `interactive` (hover/focus-within lift); `as` prop for the element tag. **`Tag.astro`** — maps a taxonomy key → localized label via new `tags.*` dict entries (added to both en/ko.json for all 9 tags); modes static `<span>` / link `<a>` / interactive `<button aria-pressed>` (filter chips). Focus-visible inherited from the global ring. `astro check` 0 errors. Next: Stage 12 (Callout, Timeline, Code, TOC).
>
> **Note:** Stage 10 completed on 2026-06-28 — collections seeded end-to-end. **Blog:** 2 EN + 2 KO posts, all non-draft, paired by `translationKey` (`agent-readiness`, `welcome`) — KO authored natively (not MT) with a `TODO(daniel)` voice-refine marker. **Portfolio:** 6 EN + 6 KO entries (AI-knowledge-work lead, digital-garden, talks/DevRel, Multi-DRM, watermarking, career) mirrored by `translationKey`; **3 featured per locale** (ai-knowledge-work, digital-garden, multi-drm) for Home; KO summaries are `TODO(daniel)` native stubs. **Timeline:** 4 EN + 4 KO JSON entries (engineer → PO → PM → DevRel). About prose → `src/data/about.ts` (EN seed from §13.1; KO `TODO(daniel)` stub). Positioning one-liners were already in `consts.ts`/`home.ts`. **Verified:** `astro check` 0 errors; `astro build` succeeds (7 pages); 23 files carry flagged `TODO(daniel)` gaps (metrics, exact dates/orgs, KO native copy) — nothing fabricated. Next: Stage 11 (core components A).
>
> **Note:** Stage 09 completed on 2026-06-28 — full content model in `src/content.config.ts`: **blog**, **portfolio** (md/mdx), and **timeline** (json/yaml data) collections with Zod schemas matching PRD §9.1–9.3. **Two separate exported tag enums** — `BLOG_TAGS` (AI knowledge-work only) and `PORTFOLIO_TAGS` (media-tech `drm-content-security`/`ott-streaming`/`cloud-saas` + the blog tags for crossover); media-tech is kept OUT of the blog enum. Also exports `PORTFOLIO_CATEGORIES` + `Lang`/`BlogTag`/`PortfolioTag`/`PortfolioCategory` types for reuse by Stage 11/15/16. **Locale strategy: folder-by-locale (`blog/{en,ko}`, `portfolio/{en,ko}`) + explicit `lang` field**; timeline flat + `lang`. `src/utils/readingTime.ts` computes whole-minute estimates (EN by words/200, KO by chars/500, ≥1). Legacy `about.astro`/`BlogPost.astro` patched to satisfy the stricter blog Props (pass `lang`/`tags`/`draft`) — fully reworked in 14/16/17. **Verified:** `astro check` 0 errors; a throwaway off-taxonomy tag (`ott-streaming` in a blog post) failed the build with a clear enum error, valid entry passed. Next: Stage 10 (seed content).
>
> **Note:** Stage 08 completed on 2026-06-28 — **Phase 1 (Foundation) complete.** `Footer.astro` rewritten as the MiniMax dark footer (DESIGN §2): pinned charcoal `#181e25` bg + light-on-dark text in **both** themes (verified via Playwright — bg identical light/dark), i18n tagline + built-with line, dynamic © year, and a secondary language + theme toggle for parity. Social row is **LinkedIn + email + RSS only** — `consts.ts` `SOCIAL_LINKS` now holds the real LinkedIn URL and a new `CONTACT_EMAIL`; X/GitHub omitted entirely per the locked decision (no invented URLs). `Header` + `Footer` both wired into BaseLayout's slots (placeholders gone). RSS link points at root `/rss.xml` with a Stage-18 TODO for per-locale feeds. `astro check` 0 errors, build succeeds. Next: Stage 09 (content collections & schemas) — Phase 2.
>
> **Note:** Stage 07 completed on 2026-06-28 — header chrome is live. `Header.astro` (sticky pill nav per DESIGN-minimax §4) renders Home·About·Portfolio·Blog with i18n labels + `getLocalizedPath` links + `aria-current` active state; collapses behind a hamburger < 768px (focus-trapped, Esc-closes, returns focus, first-link focus on open). `ThemeToggle.astro` honors the Stage 03 contract (writes `localStorage.theme` + `data-theme`, dispatches `theme-change`; `aria-pressed` + label sync across instances). `LanguageToggle.astro` is a real link to the mirrored counterpart path (optional `href` override for pages without a 1:1 translation; persists `localStorage.lang`; `hreflang`/`lang` semantics). Header rendered via BaseLayout's `header` slot default. Added `nav.primaryLabel` key to both dicts. **Verified live in a real browser (Playwright):** theme toggle flips/persists/reverts; mobile menu opens (focus→first link, `aria-expanded`/label flip), Esc closes. `astro check` 0 errors. NOTE: language toggle target `/ko/...` 404s until Stage 13 builds the KO routes — expected. Next: Stage 08 (footer & global chrome).
>
> **Note:** Stage 06 completed on 2026-06-28 — `src/layouts/BaseLayout.astro` is now the canonical page shell: props `title`/`description`/`lang`/`ogImage?`/`noindex?`; sets `<html lang>` per locale, renders `BaseHead` (no-flash `ThemeScript` in head), a skip-to-content link (first focusable → `#main`), and `header`/`main#main`/`footer` landmarks. Header/footer are **placeholder named slots** (real chrome lands Stage 07/08). `BaseHead` gained a `noindex` prop + an hreflang TODO hook for Stage 18. Added a `.container`/`.container--prose` utility + a global `:focus-visible` ring to `global.css`; body is a flex column for a sticky footer. `index.astro` migrated onto the shell as a minimal placeholder (full home = Stage 13). Verified `astro check` (0 errors) + `dist`: `<html lang="en">`, skip link first in `<body>`, landmarks in order, no `noindex`. Next: Stage 07 (header nav + toggles).
>
> **Note:** Stage 05 completed on 2026-06-28 — i18n plumbing landed in `src/i18n/`: `en.json` + `ko.json` UI dictionaries (identical key shape, nested-by-surface: `nav`/`lang`/`theme`/`footer`/`blog`/`portfolio`/`home`/`about`/`notFound`/`common`) and `utils.ts` (`Lang` type, `getLangFromUrl`, `useTranslations`→`t(key, params?)` with dot-path lookup + `{token}` interpolation + EN fallback, `getLocalizedPath` (root for en, `/ko` prefix for ko, idempotent), `getAltLocale`, `formatDate`, `formatReadingTime`). Key parity enforced at compile time via `ko satisfies typeof en`. Verified `astro check` (0 errors) + runtime values via tsx. Next: Stage 06 (base layout shell).
>
> **Note:** Stage 04 completed on 2026-06-28 — fonts wired via Astro's `<Font>` (from `astro:assets`) in `BaseHead`: DM Sans + Pretendard `preload`ed, Outfit/Poppins/Roboto on demand. Each `<Font>` defines its `--font-*` var (consumed by the role vars bound in `tokens.css` back in Stage 02). Korean handled via `:lang(ko)` swapping body/heading roles to Pretendard, plus `body:lang(ko)` reading tweaks (1.7 leading, −0.01em tracking, `word-break: keep-all`). `optimizedFallbacks` emits capsize-metric fallback faces → no CLS. Verified in `dist`: 5 `--font-*` vars resolve to real faces, preloads limited to the two primary faces, `:lang(ko)` rules present. Next: Stage 05 (i18n utilities & UI dictionaries).

**Critical path:** 01 → 02 → 03/04 → 05 → 06 → 07 → 08, then 09 → 10 in parallel with 11 → 12, converging on pages 13–18, finishing with 19/20 → 21.

## Not yet decomposed (P1/P2 backlog — PRD §12)

- **P1:** tag/topic archive pages, blog search, pagination/load-more, portfolio detail pages, auto-generated per-post OG images, 404 polish, Cloudflare Web Analytics, authoring docs + content-lint CI.
- **P2:** newsletter capture, post series/collections, related posts, reading progress, webmentions/external discussion links, optional hybrid/SSR, light PKM→site hand-off automation.

> When P0 ships, run this same decomposition over the P1 list to create `stage-22+`.
